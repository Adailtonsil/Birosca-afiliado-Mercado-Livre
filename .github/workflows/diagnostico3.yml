name: Diagnostico 3 (temporario)
 
on:
  workflow_dispatch: {}
 
jobs:
  diagnosticar3:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar o código do repositório
        uses: actions/checkout@v4
 
      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
 
      - name: Executar diagnostico 3
        run: node diagnostico3.js
